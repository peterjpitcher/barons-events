**Findings**
1. `Medium` | `High confidence` — Venue default-manager changes would not be auditable.
Evidence: venue mutations in [src/actions/venues.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/actions/venues.ts:1) do not call the audit helper; the helper type in [src/lib/audit-log.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/audit-log.ts:7) does not allow a `venue` entity; and the DB constraint in [supabase/migrations/20260410120001_fix_audit_log_schema.sql](/Users/peterpitcher/Cursor/BARONS-BaronsHub/supabase/migrations/20260410120001_fix_audit_log_schema.sql:24) only allows `event`, `sop_template`, `planning_task`, `auth`, `customer`, and `booking`. If this field changes, there is no actor/history trail. I would log it.

2. `Low` | `High confidence` — The spec is inaccurate about venue read RLS, and the new column will inherit that broader read scope.
Evidence: the spec says venue read/write policies are scoped to `central_planner` in [docs/superpowers/specs/2026-04-14-venue-default-manager-and-table-fix-design.md](/Users/peterpitcher/Cursor/BARONS-BaronsHub/docs/superpowers/specs/2026-04-14-venue-default-manager-and-table-fix-design.md:28), but the actual `SELECT` policy is unconditional in [supabase/migrations/20250218000000_initial_mvp.sql](/Users/peterpitcher/Cursor/BARONS-BaronsHub/supabase/migrations/20250218000000_initial_mvp.sql:172). `listVenues()` selects `*` in [src/lib/venues.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/venues.ts:6), and venue rows are loaded into event flows in [src/app/events/new/page.tsx](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/app/events/new/page.tsx:43) and [src/lib/events.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/events.ts:234). This is not a write-bypass, but it does mean the new column is not planner-only at the DB read layer.

3. `Low` | `Medium confidence` — Validation should explicitly trim and null-normalize the new field, not only cap it at 200 chars.
Evidence: the analogous event field currently uses raw `z.string().max(200).optional().nullable()` in [src/lib/validation.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/validation.ts:130) and is persisted as-is in [src/actions/events.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/actions/events.ts:653) and [src/lib/events.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/events.ts:439). If the venue field follows that pattern, whitespace-only values and minor formatting variants will accumulate.

**Other checks**
- Planner-only venue management is enforced today in both layers you asked about: RLS write policy in [supabase/migrations/20250218000000_initial_mvp.sql](/Users/peterpitcher/Cursor/BARONS-BaronsHub/supabase/migrations/20250218000000_initial_mvp.sql:176) and server actions in [src/actions/venues.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/actions/venues.ts:36), [src/actions/venues.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/actions/venues.ts:74), and [src/actions/venues.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/actions/venues.ts:122). The page gate is also planner-only in [src/app/venues/page.tsx](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/app/venues/page.tsx:18).
- No extra write RLS policy is required just because a new column is added; venue RLS is table-level.
- `200` chars is sufficient for a human-name/free-text responsibility field and matches the existing event constraint in [supabase/migrations/20260408120002_add_event_planning_link.sql](/Users/peterpitcher/Cursor/BARONS-BaronsHub/supabase/migrations/20260408120002_add_event_planning_link.sql:12). It does not affect XSS risk.
- XSS risk looks low in the current rendering path. The value is rendered into a normal React `<Input>` in [src/components/events/event-form.tsx](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/components/events/event-form.tsx:766), which is escaped, and the read-only detail view uses [src/components/events/event-detail-summary.tsx](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/components/events/event-detail-summary.tsx:57) without rendering `manager_responsible`.
- Free text with no FK is consistent with the existing `events.manager_responsible` model. That is acceptable only if this venue field is treated as a convenience default, not an authoritative identity, because it will go stale when managers change and can then propagate stale names into future events.