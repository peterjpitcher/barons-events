**Assumption Breaker Report**

**Medium**
- `AB-01` `last_activity_at` is not just harmless dead metadata. The app no longer renews or enforces it, but the schema still documents idle-timeout behavior, keeps an index for it, and defines `cleanup_auth_records()` to delete sessions idle for 30+ minutes. App code also still writes and returns the field even though nothing in `src/` consumes it beyond a test. Refs: [20260311100000_auth_session_tables.sql](/Users/peterpitcher/Cursor/BARONS-BaronsHub/supabase/migrations/20260311100000_auth_session_tables.sql#L2), [20260311100000_auth_session_tables.sql](/Users/peterpitcher/Cursor/BARONS-BaronsHub/supabase/migrations/20260311100000_auth_session_tables.sql#L17), [20260311100000_auth_session_tables.sql](/Users/peterpitcher/Cursor/BARONS-BaronsHub/supabase/migrations/20260311100000_auth_session_tables.sql#L38), [session.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/auth/session.ts#L58), [session.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/auth/session.ts#L107). I found no current app-layer invocation of `cleanup_auth_records()`, so this is drift rather than an active runtime bug.
- `AB-02` Assumption 4 is unproven. The code now enforces only a 24-hour absolute expiry; there is no inactivity invalidation path left. Refs: [session.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/auth/session.ts#L7), [session.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/auth/session.ts#L95), [middleware.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/middleware.ts#L228). Inference: this is a security-policy downgrade versus idle expiry, and “sufficient” needs explicit product/security sign-off rather than code-level confidence.
- `AB-03` Assumption 6 is only partially true. The updated tests pass, but the expired-session test is masking a bad mock setup: `validateSession()` now logs absolute expiry, and the test does not mock the audit insert path. The suite stays green because `logAuthEvent()` swallows the error. Refs: [session.test.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/auth/__tests__/session.test.ts#L289), [audit-log.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/audit-log.ts#L93). When I ran the targeted suite, this test emitted `Auth audit log failed: TypeError: db.from(...).insert is not a function`.

**Low**
- `AB-04` Idle logout UI is now effectively unreachable from live flows, but it still exists. The login page still renders an inactivity banner for `reason=idle`, while middleware only emits `session_missing`, `session_expired`, and `session_mismatch`, and the shell sign-out forms submit `signOutAction` with `FormData` only. Refs: [login/page.tsx](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/app/login/page.tsx#L62), [middleware.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/middleware.ts#L216), [middleware.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/middleware.ts#L230), [middleware.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/middleware.ts#L246), [app-shell.tsx](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/components/shell/app-shell.tsx#L127), [auth.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/actions/auth.ts#L177). If some future caller passes `"idle"` to `signOutAction`, it only changes the redirect query string and still logs `auth.logout`.
- `AB-05` The idle-expired audit event is dead contract surface. It remains in the TypeScript union, in tests, and in the SQL audit action allowlist, but no runtime code emits it anymore; only absolute expiry is logged. Refs: [audit-log.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/audit-log.ts#L53), [audit.test.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/auth/__tests__/audit.test.ts#L184), [20260410120001_fix_audit_log_schema.sql](/Users/peterpitcher/Cursor/BARONS-BaronsHub/supabase/migrations/20260410120001_fix_audit_log_schema.sql#L77), [session.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/auth/session.ts#L99).
- `AB-06` The cron cleanup route comment is now misleading. It says the route removes “expired and idle” sessions, but the implementation it calls only deletes by `expires_at`. Refs: [cleanup-auth/route.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/app/api/cron/cleanup-auth/route.ts#L8), [session.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/auth/session.ts#L144).

**Assumption Verdicts**
- `1.` Verified for in-repo executable code. I found no live `src/` references to `renewSession` or `IDLE_TIMEOUT_MS`.
- `2.` Verified for in-repo executable code. I found no live `src/` references to `/api/auth/heartbeat`; the deleted hook/provider/route are gone.
- `3.` False as stated. `last_activity_at` creates schema and contract drift; it is not harmless in the repo’s current shape.
- `4.` Not verifiable from code. It is a security posture decision, not an implementation fact.
- `5.` Partially false. Runtime wiring is clean, but idle-related UI, audit, comment, and schema leftovers remain.
- `6.` Partially false. The targeted suite is green, but one updated test is not correctly asserting the new expiry-side effect.

**Verification**
- `rg -n "idle|heartbeat|renewSession|IDLE_TIMEOUT|reason=idle|auth\\.session\\.expired\\.idle" src/` matched only the idle banner, idle audit type/test, the cron comment, and unrelated local `"idle"` UI state.
- `rg` found no live `src/` references to `renewSession`, `IDLE_TIMEOUT_MS`, `/api/auth/heartbeat`, `useIdleTimeout`, or `IdleTimeoutProvider`.
- Deleted files were confirmed absent from disk.
- `npx vitest run src/lib/auth/__tests__/session.test.ts src/lib/auth/__tests__/audit.test.ts src/lib/auth/__tests__/invite.test.ts` passed `48/48`, with the single stderr noted above.