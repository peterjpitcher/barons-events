1. `Medium` | `High confidence`  
The edit form does not pre-fill `manager_responsible` when an existing event has that field blank. The mount effect is gated to create mode in [event-form.tsx](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/components/events/event-form.tsx:448), but the edit screen renders the same form with `mode="edit"` in [page.tsx](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/app/events/[eventId]/page.tsx:591). That contradicts the approved behavior in the spec, which says to pre-fill whenever `manager_responsible` is empty and a venue is selected in [the spec](/Users/peterpitcher/Cursor/BARONS-BaronsHub/docs/superpowers/specs/2026-04-14-venue-default-manager-and-table-fix-design.md:79).

2. `Medium` | `High confidence`  
The venues table “alignment fix” is still misaligned because the grid child order does not match the header order. The header says `Hours` then `Actions` in [venues-manager.tsx](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/components/venues/venues-manager.tsx:123), but grid item 5 is the Save button in [venues-manager.tsx](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/components/venues/venues-manager.tsx:228) and grid item 6 contains the Hours link plus Delete action in [venues-manager.tsx](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/components/venues/venues-manager.tsx:239). Because the form uses `className="contents"`, Save renders under the `Hours` column and the clock/delete controls share the `Actions` column.

3. `Medium` | `Medium confidence`  
This feature introduces a new privacy/RLS exposure path by storing a human-readable staff name on `public.venues`, while that table is still globally readable. The new column is added in [20260414120000_add_venue_default_manager.sql](/Users/peterpitcher/Cursor/BARONS-BaronsHub/supabase/migrations/20260414120000_add_venue_default_manager.sql:2), and `venues` still has `for select using (true)` in [20250218000000_initial_mvp.sql](/Users/peterpitcher/Cursor/BARONS-BaronsHub/supabase/migrations/20250218000000_initial_mvp.sql:172). The repo also exposes a browser anon Supabase client in [client.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/supabase/client.ts:12). Inference: unless manager names are intentionally public, anonymous callers can now read them directly from Supabase.

4. `Low` | `High confidence`  
The generated Supabase types are still not aligned with the schema the spec called for. `venues.Row` now includes `default_manager_responsible` in [types.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/supabase/types.ts:23), but `events.Row` still does not include the already-migrated `manager_responsible` field in [types.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/supabase/types.ts:69), despite the explicit requirement in [the spec](/Users/peterpitcher/Cursor/BARONS-BaronsHub/docs/superpowers/specs/2026-04-14-venue-default-manager-and-table-fix-design.md:114). The new form code has to fall back to `as any` in [event-form.tsx](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/components/events/event-form.tsx:322), which is a repo-convention miss and weakens type safety around this feature.

The migration itself is additive and non-destructive. I did not find a new XSS sink or a server-action auth bypass; the only security concern I found is the public-read/RLS issue above.

I ran `npx vitest run src/actions/__tests__/venues.test.ts` and a targeted `eslint` pass on the touched files; both passed. The new tests only cover venue action payload wiring, so the event-form auto-populate path and the table column ordering remain untested.