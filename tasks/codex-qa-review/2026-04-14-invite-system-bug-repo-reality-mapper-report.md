**Findings**
- `ResendInviteButton` is genuinely nested inside outer user-edit forms in both responsive layouts. Mobile authored tree: `Card > CardContent > form(updateUserAction) > ... > div > ResendInviteButton > form(resendInviteAction) > button` at [users-manager.tsx](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/components/users/users-manager.tsx:178), [users-manager.tsx](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/components/users/users-manager.tsx:210), and [resend-invite-button.tsx](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/components/users/resend-invite-button.tsx:34). Desktop authored tree: `li > form(updateUserAction) > div > ResendInviteButton > form(resendInviteAction) > button` at [users-manager.tsx](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/components/users/users-manager.tsx:280) and [users-manager.tsx](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/components/users/users-manager.tsx:305). That is invalid HTML and a strong candidate for the resend failure.
- `sendInviteEmail` does double-escape names. It builds `greeting` with `escapeHtml(fullName)` at [notifications.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/notifications.ts:386), then passes that into `intro`, and `renderEmailTemplate` escapes `intro` again at [notifications.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/notifications.ts:46). Names containing `&`, `<`, `'`, etc. will be mangled.
- `resendInviteAction` trusts client-hidden `email` and `fullName` instead of loading them by `userId`. It checks confirmation status by `userId`, then generates the new invite link for the submitted `email` at [users.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/actions/users.ts:227) and [users.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/actions/users.ts:238). That is an overlooked integrity bug.
- `inviteUserAction` can create the auth user and upsert the `users` row, then still return `success: false` if email sending fails at [users.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/actions/users.ts:146). `InviteUserForm` only refreshes on `state.success` at [users-manager.tsx](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/components/users/users-manager.tsx:63), so the UI can report failure even though the user now exists.

**File map**
- `src/components/users/users-manager.tsx`: `InviteUserForm` is standalone and not nested at [users-manager.tsx](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/components/users/users-manager.tsx:81). The resend button is nested inside outer update forms in both `UserCardMobile` and `UserDesktopRow`.
- `src/components/users/resend-invite-button.tsx`: yes, it renders its own `<form action={formAction}>` at [resend-invite-button.tsx](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/components/users/resend-invite-button.tsx:34).
- `src/actions/users.ts`: both actions are mostly structurally sound if called directly. `inviteUserAction` does auth check, Zod parse, `generateLink(type: "invite")`, `users` upsert, email send, audit, revalidate. `resendInviteAction` does auth check, confirmation check via `getUserById`, fresh invite-link generation, email send, audit.
- `src/lib/notifications.ts`: double-escaping bug is real. `getResendClient()` returns `null` if `RESEND_API_KEY` is absent at [notifications.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/notifications.ts:204).
- `src/lib/app-url.ts`: `resolveAppUrl()` is `NEXT_PUBLIC_SITE_URL ?? NEXT_PUBLIC_APP_URL ?? https://${VERCEL_URL} ?? "https://baronshub.orangejelly.co.uk"` at [app-url.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/app-url.ts:5). In this repo’s `.env.local`, the effective value is `https://barons-events.vercel.app`.
- `src/app/auth/confirm/route.ts`: it does handle invite tokens. It accepts `token_hash` or `code`, verifies/exchanges them, and redirects invite/recovery flows to `/reset-password` at [route.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/app/auth/confirm/route.ts:28) and [route.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/app/auth/confirm/route.ts:62). The comment mentioning `/auth/update-password` is stale.
- `middleware.ts`: `/auth/confirm` and `/reset-password` are public at [middleware.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/middleware.ts:13). Nothing here obviously blocks invite confirmation or authenticated server-action posts.
- `next.config.ts`: only `experimental.serverActions.bodySizeLimit = "10mb"` at [next.config.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/next.config.ts:4). No invite-specific behavior.
- `src/lib/env.ts`: `RESEND_API_KEY` is optional in schema at [env.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/lib/env.ts:3). In the checked local repo config, it is present.
- Other invite-related code Claude missed: password setup goes through `/reset-password`, and `completePasswordResetAction` updates the password, rotates app sessions, then signs the user out to send them back to login at [auth.ts](/Users/peterpitcher/Cursor/BARONS-BaronsHub/src/actions/auth.ts:265).

**Claude claim verdicts**
- `1. Nested resend form is why resend invites do not work`: `CONFIRMED` on the code fact. The nesting exists in both layouts and is a real bug. I did not execute the page, so this is code-level confirmation rather than a browser trace.
- `2. Double HTML escaping in sendInviteEmail`: `CONFIRMED`.
- `3. InviteUserForm is standalone and the issue is likely email delivery config`: `PARTIALLY CORRECT`. The form is standalone, but there are other real code issues besides delivery config, and this repo already has a `RESEND_API_KEY` configured. A sender/domain delivery problem is still possible, but it is not the only plausible issue.