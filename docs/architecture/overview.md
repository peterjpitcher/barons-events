---
generated: true
last_updated: 2026-04-23T12:30:00Z
source: session-setup
project: barons-events-mvp
---

# Project Overview

> Auto-generated by session-setup. Manual edits will be overwritten on next refresh.

## Summary

**Barons Events Workspace** — an internal event management platform for Barons pub group staff. Staff plan, review, and learn from events in one shared workspace. A public REST API exposes event data to the external website.

## Stack

| Layer | Technology |
|---|---|
| Framework | Next.js 16.1, App Router, React 19.1 |
| Language | TypeScript (strict) |
| Styling | Tailwind CSS v4 |
| Database | Supabase PostgreSQL + RLS |
| Auth | Supabase Auth + custom app-session-id layer |
| Email | Resend |
| SMS | Twilio |
| AI | OpenAI (gpt-4o-mini) |
| QR Codes | `qrcode` library |
| Short Links | Custom `/l/[slug]` + `l.baronspubs.com` domain |
| Rate Limiting | Upstash Redis (KV_REST_API_URL) |
| CAPTCHA | Cloudflare Turnstile |
| Deployment | Vercel |

## Stats

| Metric | Count |
|---|---|
| Pages | 30 (25 authenticated, 5 public incl. `/l/[slug]`) |
| API routes | 21 (8 public v1, 9 cron, 2 auth, 1 webhook, 1 short-link) |
| Server actions | 80+ across 16 domain files |
| Integrations | 7 (Resend, Twilio, OpenAI, Upstash, Turnstile, Supabase Storage, QR) |
| Action files | 16 in `src/actions/` |

## Roles

| Role | Tier | Capabilities |
|---|---|---|
| `administrator` | Admin | Full platform access, user management |
| `office_worker` | Editor | Venue-scoped write (venue_id set) or global read-only |
| `executive` | Viewer | Read-only — events, planning, reporting |

Role stored in `public.users.role`. Capability helpers in `src/lib/roles.ts` (16 capability functions).

## Key Sections

- [[routes]] — All pages, API routes, and cron jobs
- [[server-actions]] — All server-side mutations
- [[data-model]] — Database schema reference
- [[relationships]] — Cross-reference map of actions, tables, routes, integrations

## Entry Points

| Path | Purpose |
|---|---|
| `middleware.ts` | Auth gate, session validation, CSP headers, short-link rewriting |
| `src/app/layout.tsx` | Root layout — loads `getCurrentUser()`, renders `AppShell` |
| `src/lib/auth.ts` | `getCurrentUser()`, `requireAuth()`, `withAuth()`, `withAdminAuth()`, CSRF wrappers |
| `src/lib/auth/session.ts` | Custom session validation (app-session-id cookie) |
| `src/lib/roles.ts` | RBAC capability functions |
| `src/lib/public-api/` | Rate-limited REST API for external website |
| `src/lib/cron-auth.ts` | CRON_SECRET bearer token verification |

## Environment Variables

| Var | Public/Server | Purpose |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | Public | Supabase project URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Public | Supabase anon key |
| `NEXT_PUBLIC_SITE_URL` | Public | App base URL for emails |
| `NEXT_PUBLIC_TURNSTILE_SITE_KEY` | Public | Turnstile CAPTCHA site key |
| `SUPABASE_SERVICE_ROLE_KEY` | Server | Admin DB access |
| `RESEND_API_KEY` | Server | Email service |
| `RESEND_FROM_EMAIL` | Server | Sender address |
| `NOTIFICATIONS_DISABLED` | Server | Kill-switch for business emails |
| `BARONSHUB_WEBSITE_API_KEY` | Server | Public API auth |
| `OPENAI_API_KEY` | Server | AI content generation |
| `OPENAI_WEBSITE_COPY_MODEL` | Server | Model override (default: gpt-4o-mini) |
| `CRON_SECRET` | Server | Cron endpoint auth |
| `TWILIO_ACCOUNT_SID` | Server | SMS account |
| `TWILIO_AUTH_TOKEN` | Server | SMS auth |
| `TWILIO_FROM_NUMBER` | Server | SMS sender number |
| `TWILIO_WEBHOOK_URL` | Server | Inbound SMS webhook |
| `TURNSTILE_SECRET_KEY` | Server | CAPTCHA server validation |
| `KV_REST_API_URL` | Server | Upstash Redis URL (rate limiting) |
| `KV_REST_API_TOKEN` | Server | Upstash Redis token |
| `SHORT_LINK_HOST` | Server | Short link domain (default: l.baronspubs.com) |
