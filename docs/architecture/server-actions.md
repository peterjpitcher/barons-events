---
generated: true
last_updated: 2026-05-07T12:00:00Z
source: session-setup
project: barons-events-mvp
---

# Server Actions

> Auto-generated by session-setup. Manual edits will be overwritten on next refresh.

70+ server actions across 17 domain files in `src/actions/`. Every file begins with `"use server"`.

**Conventions:**
- Auth: most actions call `getCurrentUser()` from `@/lib/auth` (covered by [[overview#Auth Model]]); a few rely on the parent route + RBAC helper.
- Audit: most mutating actions emit `logAuditEvent` and trigger `revalidatePath` for affected URLs (linked into [[routes]]).
- RBAC capability checks come from `src/lib/roles.ts` — see [[overview#Role Model]].

For tables → actions reverse map, see [[relationships#Tables to Actions]].
For action → page-caller map, see [[relationships#Actions to Pages]].

## Auth Actions

File: `src/actions/auth.ts` — no audit/revalidatePath calls (auth flows handle their own redirects).

| Action | Tables Touched | Auth Check | Audit Logged |
|---|---|---|---|
| `signInAction` | `users` | password verify | redirect-only |
| `signOutAction` | (session teardown) | session | redirect-only |
| `requestPasswordResetAction` | `users` | rate-limited | redirect-only |
| `completePasswordResetAction` | `users` | token | redirect-only |

## Event Actions

File: `src/actions/events.ts` — touches [[data-model#events|events]], [[data-model#users|users]], [[data-model#venues|venues]].

| Action | Auth Check | Audit Logged |
|---|---|---|
| `saveEventDraftAction` | `getCurrentUser` + `canEditEvent` | yes |
| `submitEventForReviewAction` | `getCurrentUser` + `canProposeEvents` | yes |
| `reviewerDecisionAction` | `getCurrentUser` + `canReviewEvents` | yes |
| `generateWebsiteCopyAction` | `getCurrentUser` | yes (OpenAI call) |
| `generateWebsiteCopyFromFormAction` | `getCurrentUser` | yes (OpenAI call) |
| `generateTermsAndConditionsAction` | `getCurrentUser` | yes (OpenAI call) |
| `updateAssigneeAction` | `getCurrentUser` | yes |
| `deleteEventAction` | `getCurrentUser` + admin only | yes |
| `revertToDraftAction` | `getCurrentUser` + `isAdministrator` | yes |
| `updateBookingSettingsAction` | `getCurrentUser` + `canEditEvent` | yes |

## Pre-Event (Proposal) Actions

File: `src/actions/pre-event.ts` — touches [[data-model#venues|venues]].

| Action | Auth Check | Audit Logged |
|---|---|---|
| `proposeEventAction` | `getCurrentUser` + `canProposeEvents` | yes |
| `preApproveEventAction` | `getCurrentUser` + admin/reviewer | yes |
| `preRejectEventAction` | `getCurrentUser` + admin/reviewer | yes |

## Booking Actions

File: `src/actions/bookings.ts` — touches [[data-model#customers|customers]], [[data-model#event_bookings|event_bookings]], [[data-model#events|events]], [[data-model#sms_campaign_sends|sms_campaign_sends]].

| Action | Auth Check | Audit Logged |
|---|---|---|
| `createBookingAction` | `getCurrentUser` + `canManageBookings(role, venueId)` | yes |
| `updateExistingBookingAction` | `getCurrentUser` + `canManageBookings` | yes |
| `cancelBookingAction` | `getCurrentUser` + `canManageBookings` | yes |

## Customer Actions

File: `src/actions/customers.ts` — touches [[data-model#customers|customers]], [[data-model#event_bookings|event_bookings]].

| Action | Auth Check | Audit Logged |
|---|---|---|
| `deleteCustomerAction` | `getCurrentUser` + `canManageCustomers` | (none) |

## Artist Actions

File: `src/actions/artists.ts` — server-side only (no `.from()` calls; uses helper from `src/lib/artists.ts`).

| Action | Auth Check | Audit Logged |
|---|---|---|
| `createArtistAction` | `getCurrentUser` + `canManageArtists` | yes |
| `updateArtistAction` | `getCurrentUser` + `canManageArtists` | yes |
| `archiveArtistAction` | `getCurrentUser` + `canManageArtists` | yes |
| `restoreArtistAction` | `getCurrentUser` + `canManageArtists` | yes |

## Debrief Actions

File: `src/actions/debriefs.ts` — touches [[data-model#business_settings|business_settings]], [[data-model#debriefs|debriefs]], [[data-model#events|events]].

| Action | Auth Check | Audit Logged |
|---|---|---|
| `submitDebriefAction` | `getCurrentUser` + `canCreateDebriefs`/`canEditDebrief` | yes |

## Planning Actions

File: `src/actions/planning.ts` — touches [[data-model#planning_inspiration_dismissals|planning_inspiration_dismissals]], [[data-model#planning_inspiration_items|planning_inspiration_items]], [[data-model#planning_items|planning_items]], [[data-model#planning_series|planning_series]], [[data-model#planning_task_assignees|planning_task_assignees]], [[data-model#planning_tasks|planning_tasks]].

| Action | Auth Check | Audit Logged |
|---|---|---|
| `createPlanningItemAction` | `getCurrentUser` + `canCreatePlanningItems` | yes |
| `updatePlanningItemAction` | `getCurrentUser` + `canManageOwnPlanningItems` | yes |
| `movePlanningItemDateAction` | `getCurrentUser` + `canManageAllPlanning` | yes |
| `deletePlanningItemAction` | `getCurrentUser` + `canManageAllPlanning` | yes |
| `createPlanningSeriesAction` | `getCurrentUser` + `canManageAllPlanning` | yes |
| `updatePlanningSeriesAction` | `getCurrentUser` + `canManageAllPlanning` | yes |
| `pausePlanningSeriesAction` | `getCurrentUser` + `canManageAllPlanning` | yes |
| `createPlanningTaskAction` | `getCurrentUser` + `canManageOwnPlanningItems` | yes |
| `updatePlanningTaskAction` | `getCurrentUser` + `canManageOwnPlanningItems` | yes |
| `togglePlanningTaskStatusAction` | `getCurrentUser` | yes |
| `reassignPlanningTaskAction` | `getCurrentUser` + `canManageOwnPlanningItems` | yes |
| `deletePlanningTaskAction` | `getCurrentUser` + `canManageOwnPlanningItems` | yes |
| `convertInspirationItemAction` | `getCurrentUser` | yes |
| `dismissInspirationItemAction` | `getCurrentUser` | yes |
| `refreshInspirationItemsAction` | `getCurrentUser` | yes |

## Attachment Actions

File: `src/actions/attachments.ts` — touches [[data-model#attachments|attachments]] (Supabase Storage).

| Action | Auth Check | Audit Logged |
|---|---|---|
| `requestAttachmentUploadAction` | `getCurrentUser` + RBAC per parent resource | yes |
| `confirmAttachmentUploadAction` | `getCurrentUser` + RBAC per parent resource | yes |
| `deleteAttachmentAction` | `getCurrentUser` + RBAC per parent resource | yes |
| `getAttachmentUrlAction` | `getCurrentUser` + RBAC per parent resource | (read-only) |

## Link / Short-Link Actions

File: `src/actions/links.ts` — server-side only (uses helper that touches `short_links`).

| Action | Auth Check | Audit Logged |
|---|---|---|
| `createShortLinkAction` | `getCurrentUser` + `canManageLinks` | yes |
| `updateShortLinkAction` | `getCurrentUser` + `canManageLinks` | yes |
| `deleteShortLinkAction` | `getCurrentUser` + `canManageLinks` | yes |
| `getOrCreateUtmVariantAction` | `getCurrentUser` + `canManageLinks` | yes |

## Event-Type Actions

File: `src/actions/event-types.ts` — settings table (touched via helper).

| Action | Auth Check | Audit Logged |
|---|---|---|
| `createEventTypeAction` | `getCurrentUser` + `canManageSettings` | yes |
| `updateEventTypeAction` | `getCurrentUser` + `canManageSettings` | yes |
| `deleteEventTypeAction` | `getCurrentUser` + `canManageSettings` | yes |

## Venue Actions

File: `src/actions/venues.ts` — touches [[data-model#pending_cascade_backfill|pending_cascade_backfill]], [[data-model#venues|venues]].

| Action | Auth Check | Audit Logged |
|---|---|---|
| `createVenueAction` | `getCurrentUser` + `canManageVenues` | yes |
| `updateVenueAction` | `getCurrentUser` + `canManageVenues` | yes |
| `deleteVenueAction` | `getCurrentUser` + `canManageVenues` | yes |

## Opening-Hours Actions

File: `src/actions/opening-hours.ts` — server-side only (touches `service_types`, `venue_opening_hours`, `venue_opening_overrides` via helpers).

| Action | Auth Check | Audit Logged |
|---|---|---|
| `createServiceTypeAction` | `getCurrentUser` + `canManageSettings` | yes |
| `updateServiceTypeAction` | `getCurrentUser` + `canManageSettings` | yes |
| `deleteServiceTypeAction` | `getCurrentUser` + `canManageSettings` | yes |
| `upsertVenueOpeningHoursAction` | `getCurrentUser` + `canManageVenues` | yes |
| `upsertMultiVenueOpeningHoursAction` | `getCurrentUser` + `canManageVenues` | yes |
| `createOpeningOverrideAction` | `getCurrentUser` + `canManageVenues` | yes |
| `updateOpeningOverrideAction` | `getCurrentUser` + `canManageVenues` | yes |
| `deleteOpeningOverrideAction` | `getCurrentUser` + `canManageVenues` | yes |

## User Actions

File: `src/actions/users.ts` — touches [[data-model#app_sessions|app_sessions]], [[data-model#approvals|approvals]], [[data-model#artists|artists]], [[data-model#audit_log|audit_log]], [[data-model#debriefs|debriefs]], [[data-model#event_artists|event_artists]], [[data-model#event_versions|event_versions]], [[data-model#events|events]], [[data-model#planning_items|planning_items]], [[data-model#planning_series_task_templates|planning_series_task_templates]], [[data-model#planning_series|planning_series]], [[data-model#planning_task_assignees|planning_task_assignees]], [[data-model#planning_tasks|planning_tasks]], [[data-model#short_links|short_links]], [[data-model#users|users]], [[data-model#venue_opening_overrides|venue_opening_overrides]], [[data-model#venues|venues]].

| Action | Auth Check | Audit Logged |
|---|---|---|
| `updateUserAction` | `getCurrentUser` + `canManageUsers` | yes |
| `inviteUserAction` | `getCurrentUser` + `canManageUsers` | yes (Resend) |
| `resendInviteAction` | `getCurrentUser` + `canManageUsers` | yes (Resend) |
| `listReassignmentTargets` | `getCurrentUser` + `canManageUsers` | (read-only) |
| `getUserImpactSummary` | `getCurrentUser` + `canManageUsers` | (read-only) |
| `deactivateUserAction` | `getCurrentUser` + `canManageUsers` | yes |
| `reactivateUserAction` | `getCurrentUser` + `canManageUsers` | yes |
| `deleteUserAction` | `getCurrentUser` + `canManageUsers` | yes |

## Business-Settings Actions

File: `src/actions/business-settings.ts` — touches [[data-model#business_settings|business_settings]].

| Action | Auth Check | Audit Logged |
|---|---|---|
| `updateBusinessSettingsAction` | `getCurrentUser` + `canManageSettings` | yes |

## SLT (Senior Leadership Team) Actions

File: `src/actions/slt.ts` — touches [[data-model#slt_members|slt_members]].

| Action | Auth Check | Audit Logged |
|---|---|---|
| `addSltMemberAction` | `getCurrentUser` + `isAdministrator` | yes |
| `removeSltMemberAction` | `getCurrentUser` + `isAdministrator` | yes |

## SOP Actions

File: `src/actions/sop.ts` — touches [[data-model#events|events]], [[data-model#planning_items|planning_items]], [[data-model#sop_sections|sop_sections]], [[data-model#sop_task_dependencies|sop_task_dependencies]], [[data-model#sop_task_templates|sop_task_templates]].

| Action | Auth Check | Audit Logged |
|---|---|---|
| `loadSopAssignableUsersAction` | `getCurrentUser` + `canViewSopTemplate` | (read-only) |
| `loadSopTemplateAction` | `getCurrentUser` + `canViewSopTemplate` | (read-only) |
| `createSopSectionAction` | `getCurrentUser` + `canEditSopTemplate` | yes |
| `updateSopSectionAction` | `getCurrentUser` + `canEditSopTemplate` | yes |
| `deleteSopSectionAction` | `getCurrentUser` + `canEditSopTemplate` | yes |
| `createSopTaskTemplateAction` | `getCurrentUser` + `canEditSopTemplate` | yes |
| `updateSopTaskTemplateAction` | `getCurrentUser` + `canEditSopTemplate` | yes |
| `deleteSopTaskTemplateAction` | `getCurrentUser` + `canEditSopTemplate` | yes |
| `createSopDependencyAction` | `getCurrentUser` + `canEditSopTemplate` | yes |
| `deleteSopDependencyByCompositeAction` | `getCurrentUser` + `canEditSopTemplate` | yes |
| `deleteSopDependencyAction` | `getCurrentUser` + `canEditSopTemplate` | yes |
| `backfillSopChecklistsAction` | `getCurrentUser` + admin | yes |
