---
generated: true
last_updated: 2026-04-23T12:30:00Z
source: session-setup
project: barons-events-mvp
---

# Server Actions

> Auto-generated by session-setup. Manual edits will be overwritten on next refresh.

All actions live in `src/actions/`. Auth is checked inside each action via `getCurrentUser()` from `src/lib/auth.ts`. All use `revalidatePath()` for cache invalidation after mutations.

## Auth (`src/actions/auth.ts`)

| Action | Purpose | Auth | Tables |
|---|---|---|---|
| `signInAction` | Email/password sign-in | Public | `users` |
| `signOutAction` | Sign out + clear session | Authenticated | — |
| `requestPasswordResetAction` | Send password reset email | Public | — (Resend) |
| `completePasswordResetAction` | Apply new password from token | Token-based | — |

## Events (`src/actions/events.ts`)

| Action | Purpose | Tables |
|---|---|---|
| `saveEventDraftAction` | Create or update event draft | `events`, `venues`, `event_types` |
| `submitEventForReviewAction` | Move event to pending review | `events`, `venues`, `users` |
| `reviewerDecisionAction` | Approve/reject event | `events`, `venues`, `users` |
| `generateWebsiteCopyAction` | AI-generate public description | `events` (+ OpenAI) |
| `generateWebsiteCopyFromFormAction` | AI-generate copy from form data | `venues` (+ OpenAI) |
| `generateTermsAndConditionsAction` | AI-generate T&Cs | `events` (+ OpenAI) |
| `updateAssigneeAction` | Reassign event to staff member | `events` |
| `deleteEventAction` | Soft-delete event | `events` |
| `revertToDraftAction` | Revert published event to draft | `events` |
| `updateBookingSettingsAction` | Update booking config | `events` |

Revalidates: `/events`, `/events/[id]`, `/reviews`, `/`

## Pre-Event (`src/actions/pre-event.ts`)

| Action | Purpose | Tables |
|---|---|---|
| `proposeEventAction` | Submit event proposal | `events`, `venues` |
| `preApproveEventAction` | Approve proposal to proceed | `events` |
| `preRejectEventAction` | Reject proposal | `events` |

Auth: `getCurrentUser()` — requires `canProposeEvents()` or admin.

## Bookings (`src/actions/bookings.ts`)

| Action | Purpose | Tables |
|---|---|---|
| `createBookingAction` | Create a booking for an event | `event_bookings`, `customers`, `sms_campaign_sends` |
| `updateExistingBookingAction` | Edit booking details | `event_bookings`, `events` |
| `cancelBookingAction` | Cancel a booking | `event_bookings`, `events` |

Revalidates: `/events/[id]/bookings`

## Debriefs (`src/actions/debriefs.ts`)

| Action | Purpose | Tables |
|---|---|---|
| `submitDebriefAction` | Submit post-event debrief | `debriefs`, `events`, `business_settings` |

Auth: `canCreateDebriefs()` or `canEditDebrief()`. Revalidates: `/events/[id]`, `/`

## Planning (`src/actions/planning.ts`)

| Action | Purpose | Tables |
|---|---|---|
| `createPlanningItemAction` | Create planning item | via RPC |
| `updatePlanningItemAction` | Edit planning item | via RPC |
| `movePlanningItemDateAction` | Reschedule item | via RPC |
| `deletePlanningItemAction` | Delete item | `planning_items`, `planning_tasks`, `planning_task_assignees` |
| `createPlanningSeriesAction` | Create recurring series | via RPC |
| `updatePlanningSeriesAction` | Edit series | via RPC |
| `pausePlanningSeriesAction` | Pause series | via RPC |
| `createPlanningTaskAction` | Add task to item | via RPC |
| `updatePlanningTaskAction` | Edit task | via RPC |
| `togglePlanningTaskStatusAction` | Complete/incomplete task | via RPC |
| `reassignPlanningTaskAction` | Reassign task owner | `planning_task_assignees`, `planning_tasks` |
| `deletePlanningTaskAction` | Delete task | via RPC |
| `convertInspirationItemAction` | Convert AI suggestion to item | `planning_inspiration_items`, `planning_items` |
| `dismissInspirationItemAction` | Dismiss AI suggestion | `planning_inspiration_dismissals` |
| `refreshInspirationItemsAction` | Trigger AI inspiration refresh | — (calls lib) |

Auth: `getCurrentUser()` — requires `canCreatePlanningItems()` or `canManageAllPlanning()`. Revalidates: `/planning`, `/`

## SOP (`src/actions/sop.ts`)

| Action | Purpose | Tables |
|---|---|---|
| `loadSopAssignableUsersAction` | List users for SOP assignment | via lib |
| `loadSopTemplateAction` | Load SOP template | via lib |
| `createSopSectionAction` | Add SOP section | `sop_sections` |
| `updateSopSectionAction` | Edit SOP section | `sop_sections` |
| `deleteSopSectionAction` | Delete SOP section | `sop_sections` |
| `createSopTaskTemplateAction` | Add task template | `sop_task_templates` |
| `updateSopTaskTemplateAction` | Edit task template | `sop_task_templates` |
| `deleteSopTaskTemplateAction` | Delete task template | `sop_task_templates` |
| `createSopDependencyAction` | Add task dependency | `sop_task_dependencies` |
| `deleteSopDependencyAction` / `deleteSopDependencyByCompositeAction` | Remove dependency | `sop_task_dependencies` |
| `backfillSopChecklistsAction` | Backfill checklists from template | `events`, `planning_items` |

Auth: `getCurrentUser()` — requires `canEditSopTemplate()`. Revalidates: `/settings`, `/planning`, `/events`

## Users (`src/actions/users.ts`)

| Action | Purpose | Tables |
|---|---|---|
| `updateUserAction` | Update user profile/role | `users` |
| `inviteUserAction` | Send invite email via Resend | `users` (+ Supabase Auth admin) |
| `resendInviteAction` | Resend invite email | `users` (+ Supabase Auth admin) |
| `listReassignmentTargets` | List users for task reassignment | `users` |
| `getUserImpactSummary` | Show what a user owns before deactivation | `events`, `planning_series`, `planning_items`, `planning_tasks`, `planning_task_assignees`, `planning_series_task_templates`, `artists`, `event_artists`, `short_links`, `venues`, `approvals`, `event_versions`, `debriefs`, `venue_opening_overrides` |
| `deactivateUserAction` | Deactivate account | `users`, `app_sessions` |
| `reactivateUserAction` | Reactivate account | `users` |
| `deleteUserAction` | Permanently delete user | `users`, `audit_log` |

Auth: `getCurrentUser()` — requires `canManageUsers()` (admin only). Revalidates: `/users`

## Venues (`src/actions/venues.ts`)

| Action | Purpose | Tables |
|---|---|---|
| `createVenueAction` | Add new venue | `venues` (via RPC) |
| `updateVenueAction` | Edit venue | `venues` |
| `deleteVenueAction` | Delete venue | `venues` |

Also touches: `pending_cascade_backfill` (on create). Revalidates: `/venues`

## Opening Hours (`src/actions/opening-hours.ts`)

| Action | Purpose |
|---|---|
| `createServiceTypeAction` | Add service type |
| `updateServiceTypeAction` | Edit service type |
| `deleteServiceTypeAction` | Delete service type |
| `upsertVenueOpeningHoursAction` | Set hours for one venue |
| `upsertMultiVenueOpeningHoursAction` | Set hours for all venues |
| `createOpeningOverrideAction` | Add one-off override |
| `updateOpeningOverrideAction` | Edit override |
| `deleteOpeningOverrideAction` | Delete override |

Revalidates: `/settings`, `/venues`, `/opening-hours`, `/venues/[id]/opening-hours`

## Artists (`src/actions/artists.ts`)

| Action | Purpose |
|---|---|
| `createArtistAction` | Add artist profile |
| `updateArtistAction` | Edit artist |
| `archiveArtistAction` | Archive artist |
| `restoreArtistAction` | Restore archived artist |

Auth: `getCurrentUser()` — requires `canManageArtists()`. Revalidates: `/artists`, `/artists/[id]`, `/events/new`, `/settings`

## Links (`src/actions/links.ts`)

| Action | Purpose |
|---|---|
| `createShortLinkAction` | Create short link |
| `updateShortLinkAction` | Edit short link |
| `deleteShortLinkAction` | Delete short link |
| `getOrCreateUtmVariantAction` | Get/create UTM variant |

Auth: `getCurrentUser()` — requires `canManageLinks()` (admin only). Revalidates: `/links`

## Attachments (`src/actions/attachments.ts`)

| Action | Purpose |
|---|---|
| `requestAttachmentUploadAction` | Get signed upload URL from Supabase Storage |
| `confirmAttachmentUploadAction` | Confirm upload completed |
| `deleteAttachmentAction` | Delete attachment + storage object |
| `getAttachmentUrlAction` | Get signed download URL |

Tables: `attachments` + Supabase Storage bucket `task-attachments`. Revalidates: `/planning`

## Event Types (`src/actions/event-types.ts`)

| Action | Purpose |
|---|---|
| `createEventTypeAction` | Add event type |
| `updateEventTypeAction` | Edit event type |
| `deleteEventTypeAction` | Delete event type |

Auth: `getCurrentUser()` — requires `canManageSettings()` (admin only). Revalidates: `/settings`

## Business Settings (`src/actions/business-settings.ts`)

| Action | Purpose | Tables |
|---|---|---|
| `updateBusinessSettingsAction` | Update global business settings | `business_settings` |

Revalidates: `/settings`

## SLT (`src/actions/slt.ts`)

| Action | Purpose | Tables |
|---|---|---|
| `addSltMemberAction` | Add user to Senior Leadership Team | `slt_members` |
| `removeSltMemberAction` | Remove user from SLT | `slt_members` |

Revalidates: `/settings`

## Customers (`src/actions/customers.ts`)

| Action | Purpose | Tables |
|---|---|---|
| `deleteCustomerAction` | Delete customer record | `customers`, `event_bookings` |

Auth: `getCurrentUser()` — requires `canManageCustomers()`. Revalidates: implicit

See also: [[routes]], [[relationships]]
