---
generated: true
last_updated: 2026-05-07T00:00:00Z
source: session-setup
project: barons-events-mvp
---

# Server Actions

> Auto-generated by session-setup. Manual edits will be overwritten on next refresh.

All server actions use `'use server'` directive. Located in `src/actions/`. Auth is enforced via `getSupabaseServerClient()` + `auth.getUser()` within the action or via shared helpers in `src/lib/`.

## Actions by Domain

### Artists (`src/actions/artists.ts`)

| Action | Tables | RPCs | Revalidates |
|--------|--------|------|-------------|
| `createArtistAction` | (via lib) | -- | `/artists`, `/events/new` |
| `updateArtistAction` | (via lib) | -- | `/artists`, `/artists/[id]`, `/events/new` |
| `archiveArtistAction` | (via lib) | -- | `/artists` |
| `restoreArtistAction` | (via lib) | -- | `/artists` |

### Attachments (`src/actions/attachments.ts`)

| Action | Tables | RPCs | Revalidates |
|--------|--------|------|-------------|
| `requestAttachmentUploadAction` | `attachments`, `task-attachments` | -- | Yes |
| `confirmAttachmentUploadAction` | `attachments`, `task-attachments` | -- | Yes |
| `deleteAttachmentAction` | `attachments`, `task-attachments` | -- | Yes |
| `getAttachmentUrlAction` | `attachments`, `task-attachments` | -- | -- |

### Auth (`src/actions/auth.ts`)

| Action | Tables | RPCs | Revalidates |
|--------|--------|------|-------------|
| `signInAction` | `users` | -- | -- |
| `signOutAction` | `users` | -- | -- |
| `requestPasswordResetAction` | `users` | -- | -- |
| `completePasswordResetAction` | `users` | -- | -- |

### Bookings (`src/actions/bookings.ts`)

| Action | Tables | RPCs | Revalidates |
|--------|--------|------|-------------|
| `createBookingAction` | `customers`, `event_bookings`, `events`, `sms_campaign_sends` | -- | Yes |
| `updateExistingBookingAction` | `customers`, `event_bookings`, `events`, `sms_campaign_sends` | -- | Yes |
| `cancelBookingAction` | `customers`, `event_bookings`, `events`, `sms_campaign_sends` | -- | Yes |

### Business Settings (`src/actions/business-settings.ts`)

| Action | Tables | RPCs | Revalidates |
|--------|--------|------|-------------|
| `updateBusinessSettingsAction` | `business_settings` | -- | Yes |

### Customers (`src/actions/customers.ts`)

| Action | Tables | RPCs | Revalidates |
|--------|--------|------|-------------|
| `deleteCustomerAction` | `customers`, `event_bookings` | -- | -- |

### Debriefs (`src/actions/debriefs.ts`)

| Action | Tables | RPCs | Revalidates |
|--------|--------|------|-------------|
| `submitDebriefAction` | `business_settings`, `debriefs`, `events` | -- | Yes |

### Event Types (`src/actions/event-types.ts`)

| Action | Tables | RPCs | Revalidates |
|--------|--------|------|-------------|
| `createEventTypeAction` | (via lib) | -- | Yes |
| `updateEventTypeAction` | (via lib) | -- | Yes |
| `deleteEventTypeAction` | (via lib) | -- | Yes |

### Events (`src/actions/events.ts`)

| Action | Tables | RPCs | Revalidates |
|--------|--------|------|-------------|
| `saveEventDraftAction` | `events`, `users`, `venues` | `set_event_venues` | Yes |
| `submitEventForReviewAction` | `events`, `users`, `venues` | `set_event_venues` | Yes |
| `reviewerDecisionAction` | `events`, `users`, `venues` | `set_event_venues` | Yes |
| `generateWebsiteCopyAction` | `events`, `users`, `venues` | -- | Yes |
| `generateWebsiteCopyFromFormAction` | `events`, `users`, `venues` | -- | Yes |
| `generateTermsAndConditionsAction` | `events`, `users`, `venues` | -- | Yes |
| `uploadEventImageAction` | `events` (+ storage) | -- | Yes |
| `deleteEventAction` | `events`, `users`, `venues` | -- | Yes |
| `revertToDraftAction` | `events`, `users`, `venues` | -- | Yes |
| `updateBookingSettingsAction` | `events`, `users`, `venues` | -- | Yes |

### Links (`src/actions/links.ts`)

| Action | Tables | RPCs | Revalidates |
|--------|--------|------|-------------|
| `createLinkAction` | (via lib) | -- | Yes |
| `updateLinkAction` | (via lib) | -- | Yes |
| `deleteLinkAction` | (via lib) | -- | Yes |
| `getOrCreateUtmVariantAction` | (via lib) | -- | -- |

### Opening Hours (`src/actions/opening-hours.ts`)

| Action | Tables | RPCs | Revalidates |
|--------|--------|------|-------------|
| `createServiceTypeAction` | (via lib) | -- | Yes |
| `updateServiceTypeAction` | (via lib) | -- | Yes |
| `deleteServiceTypeAction` | (via lib) | -- | Yes |
| `upsertVenueOpeningHoursAction` | (via lib) | -- | Yes |
| `upsertMultiVenueOpeningHoursAction` | (via lib) | -- | Yes |
| `updateOpeningOverrideAction` | (via lib) | -- | Yes |
| `createOpeningOverrideAction` | (via lib) | -- | Yes |
| `deleteOpeningOverrideAction` | (via lib) | -- | Yes |

### Planning (`src/actions/planning.ts`)

| Action | Tables | RPCs | Revalidates |
|--------|--------|------|-------------|
| `createPlanningItemAction` | `planning_items`, `planning_series`, `planning_tasks`, `planning_task_assignees`, `planning_inspiration_items`, `planning_inspiration_dismissals` | `set_planning_item_venues` | Yes |
| `updatePlanningItemAction` | (same cluster) | `set_planning_item_venues` | Yes |
| `movePlanningItemDateAction` | (same cluster) | -- | Yes |
| `deletePlanningItemAction` | (same cluster) | -- | Yes |
| `createPlanningSeriesAction` | (same cluster) | -- | Yes |
| `updatePlanningSeriesAction` | (same cluster) | -- | Yes |
| `pausePlanningSeriesAction` | (same cluster) | -- | Yes |
| `createPlanningTaskAction` | (same cluster) | -- | Yes |
| `updatePlanningTaskAction` | (same cluster) | -- | Yes |
| `togglePlanningTaskStatusAction` | (same cluster) | -- | Yes |
| `reassignPlanningTask` | (same cluster) | -- | Yes |
| `convertInspirationItemAction` | (same cluster) | -- | Yes |
| `dismissInspirationItemAction` | (same cluster) | -- | Yes |

### Pre-Event / Proposals (`src/actions/pre-event.ts`)

| Action | Tables | RPCs | Revalidates |
|--------|--------|------|-------------|
| `proposeEventAction` | `venues` | `create_multi_venue_event_proposals` | Yes |
| `preApproveEventAction` | `venues` | `pre_approve_event_proposal` | Yes |
| `preRejectEventAction` | `venues` | `reject_event_proposal` | Yes |

### SLT Members (`src/actions/slt.ts`)

| Action | Tables | RPCs | Revalidates |
|--------|--------|------|-------------|
| `addSltMemberAction` | `slt_members` | -- | Yes |
| `removeSltMemberAction` | `slt_members` | -- | Yes |

### SOP Templates (`src/actions/sop.ts`)

| Action | Tables | RPCs | Revalidates |
|--------|--------|------|-------------|
| `createSopSectionAction` | `sop_sections`, `sop_task_templates`, `sop_task_dependencies`, `events`, `planning_items` | -- | Yes |
| `updateSopSectionAction` | (same cluster) | -- | Yes |
| `deleteSopSectionAction` | (same cluster) | -- | Yes |
| `createSopTaskAction` | (same cluster) | -- | Yes |
| `updateSopTaskAction` | (same cluster) | -- | Yes |

### Users (`src/actions/users.ts`)

| Action | Tables | RPCs | Revalidates |
|--------|--------|------|-------------|
| `inviteUserAction` | `users`, `app_sessions`, + 14 more tables | `reassign_and_deactivate_user`, `reassign_user_content` | Yes |
| `updateUserAction` | (same cluster) | (same) | Yes |
| `deactivateUserAction` | (same cluster) | (same) | Yes |
| `reactivateUserAction` | (same cluster) | -- | Yes |
| `resendInviteAction` | (same cluster) | -- | Yes |

### Venues (`src/actions/venues.ts`)

| Action | Tables | RPCs | Revalidates |
|--------|--------|------|-------------|
| `createVenueAction` | `venues`, `pending_cascade_backfill` | -- | Yes |
| `updateVenueAction` | `venues`, `pending_cascade_backfill` | -- | Yes |
| `deleteVenueAction` | `venues`, `pending_cascade_backfill` | -- | Yes |

## Audit Logging

No action files call `logAuditEvent()` directly. The `audit_log` table is accessed only in `users.ts` for reassignment queries. Audit logging may be handled at the RPC/trigger level in PostgreSQL rather than in application code.

## Cross-References

- [[routes]] -- which pages call which actions
- [[data-model]] -- table and RPC definitions
- [[relationships]] -- action-to-table mapping
