---
generated: true
last_updated: 2026-04-23T12:30:00Z
source: session-setup
project: barons-events-mvp
---

# Relationships

> Auto-generated by session-setup. Manual edits will be overwritten on next refresh.

Cross-reference map: routes, actions, tables, integrations.

## Event Lifecycle

```
/events/propose → proposeEventAction → events (status: proposed)
/events/pending → preApproveEventAction / preRejectEventAction → events (status: pre_approved / rejected)
/events/new → saveEventDraftAction → events (status: draft)
/events/[eventId] → submitEventForReviewAction → events (status: pending_review)
  └─ Triggers: Resend email notification (src/lib/notifications.ts)
/reviews → reviewerDecisionAction → events (status: approved / needs_revisions / rejected)
  └─ Triggers: Resend email notification
/events/[eventId] → deleteEventAction → events (deleted_at set)
/events/[eventId] → revertToDraftAction → events (status: draft)
/events/[eventId] → updateBookingSettingsAction → events (booking config)
```

## Booking Lifecycle

```
/events/[eventId]/bookings → createBookingAction → event_bookings + customers
  └─ Optional: sms_campaign_sends record for SMS tracking
Cron triggers:
  /api/cron/sms-booking-driver → Twilio SMS confirmation (src/lib/twilio.ts)
  /api/cron/sms-reminders → Twilio SMS reminder
  /api/cron/sms-post-event → Twilio SMS follow-up (10:00 UTC daily)
updateExistingBookingAction → event_bookings
cancelBookingAction → event_bookings
```

## Planning Lifecycle

```
/planning → createPlanningItemAction / createPlanningSeriesAction → planning_items (via RPC)
/planning/[planningItemId] → createPlanningTaskAction → planning_tasks
  └─ SOP template: backfillSopChecklistsAction → events + planning_items
/api/cron/refresh-inspiration → OpenAI → planning_inspiration_items
convertInspirationItemAction → planning_items
dismissInspirationItemAction → planning_inspiration_dismissals
```

## Debrief Lifecycle

```
/debriefs/[eventId] → submitDebriefAction → debriefs + events (status: completed)
  └─ Checks business_settings for completion rules
```

## Public API Flow

```
External website (BARONSHUB_WEBSITE_API_KEY)
  → GET /api/v1/events → requireWebsiteApiKey + checkApiRateLimit (Upstash Redis)
  → Supabase anon client → events + venues (RLS: anon SELECT on published events)
  → Returns paginated JSON with cursor

Other v1 endpoints: event-types, venues, opening-times, events/by-slug/[slug]
All protected by same API key + rate limit pattern.
Health + OpenAPI spec endpoints also protected.
```

## Auth Flow

```
Browser → middleware.ts
  1. Domain rewrite: l.baronspubs.com → /l/[path]
  2. CSP nonce generation
  3. Security headers (HSTS, CSP, X-Frame-Options)
  4. Public path bypass check
  5. Supabase getUser() (JWT validation)
  6. validateSession(app-session-id) → app_sessions table
  7. Session-user binding check (anti-fixation)
  8. Deactivation check → users.deactivated_at
  9. CSRF token generation
  → Layout getCurrentUser() → users table → AppShell (role prop)
```

## Digest Email Flow

```
/api/cron/weekly-digest (Mon + Thu 8am UTC)
  → slt_members → users (recipients)
  → events (upcoming/recent)
  → Resend email (src/lib/notifications.ts)
```

## User Management Flow

```
/users → inviteUserAction → Supabase Auth admin invite + users table + Resend email
/users → deactivateUserAction → users.deactivated_at + app_sessions.delete
/users → deleteUserAction → users.delete + audit_log entry
getUserImpactSummary checks 18+ tables for ownership references
```

## Integrations Map

| Integration | Trigger | Files |
|---|---|---|
| **Resend** | User invite, password reset, event notifications, weekly digest | `src/lib/notifications.ts`, `src/actions/users.ts` |
| **Twilio SMS** | Booking confirmations, reminders, post-event | `src/lib/sms.ts`, `src/lib/twilio.ts`, cron routes |
| **Twilio Webhook** | Inbound SMS replies | `src/app/api/webhooks/twilio-inbound/route.ts` |
| **OpenAI** | Website copy generation, T&Cs, planning inspiration | `src/lib/ai.ts`, `src/lib/planning/inspiration.ts` |
| **QR Code** | UTM/short link QR generation | `src/components/links/utm-dropdown.tsx` |
| **Upstash Redis** | API rate limiting | `src/lib/redis.ts`, `src/lib/public-api/auth.ts` |
| **Cloudflare Turnstile** | CAPTCHA on login/forgot-password | `src/lib/turnstile.ts`, `src/components/turnstile-widget.tsx` |
| **Supabase Storage** | Event images + task attachments | `src/actions/attachments.ts`, `src/actions/events.ts` |

## Short Links / Landing Pages

```
l.baronspubs.com/[slug] → middleware rewrites to /l/[slug]
  → src/app/l/[slug]/page.tsx → public event landing page
l.baronspubs.com/[8-hex] → middleware passes through → src/app/[code]/route.ts → 301 redirect
```

## Tables to Actions Map

| Table | Write Actions | Read-Only Usage |
|---|---|---|
| [[data-model#events]] | saveEventDraft, submitForReview, reviewerDecision, delete, revertToDraft, updateBookingSettings, proposeEvent, preApprove, preReject | v1 API, cron/sms-*, debrief |
| [[data-model#event_bookings]] | createBooking, updateBooking, cancelBooking | cron/sms-*, customer delete |
| [[data-model#planning_items]] | create, update, move, delete, convertInspiration, backfillSop | — |
| [[data-model#users]] | updateUser, inviteUser, deactivate, reactivate, delete | middleware, layout, all actions (getCurrentUser) |
| [[data-model#venues]] | createVenue, updateVenue, deleteVenue | v1 API, events actions |

See also: [[routes]], [[server-actions]], [[data-model]]
